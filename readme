problem with excel button 
daily reoprt filter by time&date
view password transaction with time 
ai suggestion for adding more on the dashbaoard and other pages
add online icon 
add as well the alerts 

except on view password it shows the hashed one  
and keeps transltion in french
 
and keeps transltion in french

        problem with notifi 
        problem on the page of paymenst for the pending amount 
        add serch and filters with paginiation 





        rebuild the full code from the start to the end do not show me that     <!-- ... existing breadcrumb and header ... --><!-- ... rest of the dashboard content ... -->

<script>
    // ... existing JavaScript code ...


    try light design also on the modals when I click on save or print its not working still fixed also for add new do not add link just keep in form of modal rebuild the full code and remove the chart and recent activity use light colors and icons and font text add animations and transitions rebuild the full code from the start to the end with an updated version

    ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;